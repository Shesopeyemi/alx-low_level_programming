#include <stdlib.h>
#include <stdio.h>

/**
 * main - print if the number is greater than five, zero or less than six and not zero
 * Return: 0
 */
int main(void)
{
int n;
int 1;

1 = n % 10;

if (1 > 5)
{
	printf("Last digit of %d is %d and is greater than 5\n", n, 1);
}
else if (1 == 0)
{
	printf("Last digit of %d is %d and is 0\n", n, 1");
}
else (1 < 6 != 0)
{
	printf("Last digit of % d is % d and is less than 6 and not 0\n", n,1);
}
return (0);
}
